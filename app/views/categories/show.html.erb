<div class="article">
  <div class="menu-bar">
    <div class="list-container categories">
      <div class="heading">Categories</div>
      <% @categories.each do |category| %>
        <%= render partial: 'articles/category', locals: { category: category, active: category == @category }%>
      <% end %>
    </div>
    <div class="list-container sub-categories">
      <div class="heading">Sub Categories</div>
      <% @sub_categories.each do |sub_category| %>
        <%= render partial: 'articles/sub_category', locals: { sub_category: sub_category, active: false }%>
      <% end %>
    </div>
  </div>
</div>
